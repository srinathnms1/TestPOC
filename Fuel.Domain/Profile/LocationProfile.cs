namespace Fuel.Domain.Profile
{
    using Domain.ViewModel;
    using AutoMapper;

    public class LocationProfile : Profile
    {
        public LocationProfile()
        {
            //CreateMap<Test, Test>()
            //   .ForMember(src => src.Id, opt => opt.MapFrom(dist => dist.));
        }
    }
}
